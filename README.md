# Hello World GraphQl

## Como rodar o app

Use os seguintes comandos:

```
  npm install
  node index.js
```
Depois vá para `localhost:9090/graphql` e utilize a seguinte query:

```
query {
  helloWorld
}

```
